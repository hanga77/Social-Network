import { AUTH } from '../constants/actionsTypes';
import * as api from '../api/index';


export const signin =(formData, hisitory)=>async(dispatch) =>{
    try {
        //sign in the user
        hisitory.push('/')
    } catch (error) {
        
    }
} 

export const signup =(formData, hisitory)=>async(dispatch) =>{
    try {
        //sign up the user
        hisitory.push('/')
    } catch (error) {
        
    }
} 